/**
 * checkForLiveStream gets a session object and checks if it is a live stream.
 *
 * @param {Session} session
 *
 * @returns {string|null} returns sessionVideoSource value if it has a value of null it will return null
 */
export default function checkForLiveStream({ sessionVideoSource }) {
  return sessionVideoSource;
}
